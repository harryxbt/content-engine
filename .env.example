# Content Engine Configuration
# Copy this file to .env and update values as needed

# Public URL base for generated image URLs
# For local development:
PUBLIC_BASE_URL=http://localhost:8000

# For production (example):
# PUBLIC_BASE_URL=https://cdn.myserver.com

# Path overrides (optional - defaults to ./negatives, ./positives, ./output)
# NEG_ROOT=/path/to/negatives
# POS_ROOT=/path/to/positives
# OUTPUT_DIR=/path/to/output
# FONT_PATH=/path/to/font.ttf
